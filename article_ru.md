# Система позиционирования ультразвукового излучателя на базе трёх TDC7200 с фазо-временным анализом и автоматической подстройкой порога захвата ультразвуковых сигналов.



## 📌 Аннотация
Ультразвуковой контроль (UT) — метод неразрушающего контроля, широко применяемый в промышленности, особенно для контроля сварных соединений и измерения толщины. Одним из ключевых ограничений традиционного ультразвукового контроля с наклонным преобразователем, особенно при ручном сканировании является отсутствие локализации или точной локализации положения пьезоэлектрического преобразователя (ПЭП) в пространстве. Для решения этой задачи предложена архитектура системы позиционирования ПЭП, позволяющая регистрировать его координаты при каждом измерении.

---

## 📑 Содержание

1. [Введение](#1-введение)
2. [Микроконтроллер и архитектура системы](#2-микроконтроллер-и-архитектура-системы)
3. [Режим работы TDC7200 и диапазон измерений](#3-режим-работы-tdc7200-и-диапазон-измерений)  
4. [Работа с пачкой ультразвуковых импульсов](#4-работа-с-пачкой-ультразвуковых-импульсов)  
5. [Сохранение и анализ данных с TDC7200 (на Arduino Due)](#5-сохранение-и-анализ-данных-с-tdc7200-на-arduino-due)
6. [Определение скорости звука в воздухе](#6-определение-скорости-звука-в-воздухе)
7. [Применения в контроле и позиционировании](#7-применения-в-контроле-и-позиционировании)  
8. [Алгоритмы и логика работы](#8-алгоритмы-и-логика-работы)  
9. [Преимущества системы](#9-преимущества-системы)  
10. [Ограничения и особенности эксплуатации](#10-ограничения-и-особенности-эксплуатации)  
11. [Заключение](#11-заключение)   
  [Связанные материалы](#связанные-материалы)   
  [Автор](#автор)  

---

# Система на трёх TDC7200 с фазово-временным анализом и автоматической подстройкой порога захвата ультразвуковых сигналов

## 1. Введение

Позиционирование ультразвукового пьезоэлектрического преобразователя (ПЭП) — одного из ключевых элементов неразрушающего контроля металлов — играет решающую роль при оценке сварных соединений и в других задачах, где важно отличить реальные дефекты от ложных отражений. Такие ПЭП обычно работают в диапазоне **1–15 МГц** и используются для измерения толщины, поиска дефектов и других задач контроля.

Однако при ручном сканировании точное положение ПЭП в пространстве неизвестно, что затрудняет интерпретацию результатов. Для решения этой проблемы предложена вспомогательная **система позиционирования**, использующая ультразвуковой излучатель частотой **40 кГц**, прикреплённый к ПЭП (основной системе) и массив приёмников, размещённых вне зоны контроля. По времени пролёта сигнала от излучателя к приёмникам определяется положение ПЭП.

В отличие от базовых ToF-систем, представленное решение реализует **комбинированный фазово-временной анализ**, фиксируя как моменты фронта, так и спада сигнала. Это позволяет не только оценить время прихода, но и **уточнить фазу сигнала**, компенсируя ошибки, вызванные изменением амплитуды, порога и формы сигнала.

Ключевым элементом системы является высокоточная микросхема измерения времени TDC-GPX2, способная регистрировать до 8 событий STOP на каждом из 4 каналов от одного общего сигнала START, с разрешением до 10 пс и максимальным временным интервалом измерения до 16 секунд. Это позволяет фиксировать целую пачку ультразвуковых импульсов, анализировать как фронты, так и спады, и выполнять фазовую коррекцию, повышающую точность позиционирования.

> Благодаря использованию LVDS-интерфейсов и управлению по SPI, архитектура остаётся надёжной и простой в реализации, а также легко масштабируемой: одна микросхема TDC-GPX2 поддерживает до 4 независимых STOP-каналов, а при необходимости возможно объединение нескольких чипов для построения 2D и 3D-систем позиционирования ультразвукового преобразователя (ПЭП).

Такой подход позволяет достичь субмиллиметровой точности позиционирования даже при наличии искажений сигнала, обеспечивая надёжную интерпретацию данных контроля.

## 2. Аппаратная архитектура системы

### 2.1. Общая структура

Полная архитектура охватывает весь путь прохождения сигнала — от генерации ультразвука до его приёма и точного измерения времени распространения:

```
Arduino Due → MOSFET → Излучатель → Воздух → Приемник → Усилитель → Компаратор → Одновибраторы → SN65LVDS2 → TDC-GPX2 → SPI → Arduino Due
```

*Рис. 1. Общая схема архитектуры: Arduino Due, TDC-GPX2, компаратор, одновибраторы, драйверы LVDS, сенсоры окружающей среды*

### 🛡️ 2.2. Ультразвуковой излучатель

Излучатель (например, MA40S4S) формирует пачку ультразвуковых импульсов частотой **40 кГц**, возбуждаемых драйвером L9110 под управлением Arduino. Поддерживаются два режима возбуждения:

* **Однотактный** — один вывод излучателя на импульсы, второй — на постоянный LOW.
* **Двухтактный (антифазный)** — на оба вывода подаются противоположные импульсы для увеличения амплитуды.

> Переключение режимов осуществляется через цифровые выходы Arduino.

### 📅 2.3. Приёмник

Приёмник (например, MA40S4R) подключается к многоступенчатому усилителю на базе OPA4134. Усиление ограничено так, чтобы уровень сигнала не превышал **3.3 В**.

### ⚖️ 2.4. Компаратор

Высокоскоростной компаратор TLV3501 формирует цифровой сигнал, переходящий порог. Порог задаётся ЦАП Arduino. Выход компаратора подаётся на два одновибратора: для фронта и спада.

### ⌛ 2.5. Одновибраторы

Используется микросхема 74HC123. Один одновибратор срабатывает на **восходящий фронт** (0°...90°), второй — на **нисходящий** (90°...180°). Каждый формирует короткий импульс длительностью \~50–100 нс.

### 🔁 2.6. LVDS-драйвер

Сигналы от одновибраторов поступают на **SN65LVDS2** — двухканальный LVDS-драйвер. Он формирует дифференциальные пары, соответствующие требованиям входов STOP микросхемы TDC-GPX2 (LVDS).

### ⏱️ 2.7. TDC-GPX2

Микросхема измерения времени TDC-GPX2 принимает START-сигнал (через SPI или отдельный вход) и до **8 STOP-событий на каждый из 4 каналов**. Результаты хранятся во внутреннем FIFO и считываются через SPI-интерфейс. Разрешение достигает **10 пс**, диапазон измерений — до **16 секунд**.

> Использование GPX2 позволяет избавиться от логики D-триггера, а также упростить обработку пачек.

### 🧠 2.8. Микроконтроллер (Arduino Due)

Arduino Due выполняет роль центрального контроллера:

* **SPI** — конфигурация и чтение данных с TDC-GPX2;
* **DAC** — установка порога компаратора;
* **I²C** — работа с датчиком температуры/влажности;
* **GPIO** — управление драйвером, подача START, индикация.

### 🌡️ 2.9. Датчик температуры и влажности

Цифровой датчик (например, SHT31) подключается по I²C. Его данные позволяют уточнять скорость звука в воздухе в зависимости от условий среды.

---

## 3. Режим работы TDC-GPX2

### 3.1 Конфигурация режимов и каналов

Микросхема **TDC-GPX2** поддерживает многоканальные измерения времени между сигналами START и STOP с высокой точностью. Архитектура системы предусматривает использование **4 независимых каналов**, каждый из которых способен регистрировать **до 8 событий STOP** на один общий сигнал START.

| Кол-во STOP-событий | Каналы | Точность (LSB) | Описание применения                       |
| ------------------- | ------ | -------------- | ----------------------------------------- |
| 4                   | до 4   | до 10 пс       | Высокоточная работа с фазовым анализом    |
| 8                   | до 4   | 10–65 пс       | Стандартный режим пачки импульсов         |
| 16                  | 1      | 10–65 пс       | Простой режим анализа ToF (одноканальный) |


### 🧮 3.2 Настройка точности измерений (LSB и REFCLK\_DIVISIONS)

TDC-GPX2 позволяет задать шаг временного измерения (LSB) через параметр `REFCLK_DIVISIONS`, который определяет, на сколько частей делится период референсного сигнала.

| LSB (пс)  | REFCLK\_DIVISIONS @ 16 МГц | Комментарий                                        |
| --------- | -------------------------- | -------------------------------------------------- |
| 1 пс      | 62500                      | Максимальная точность, фазовый анализ              |
| 10 пс     | 6250                       | Повышенная точность                                |
| **20 пс** | **3125**                   | Оптимум для задач ультразвукового позиционирования |
| 62.5 пс   | 1000                       | Один такт референсного сигнала (16 МГц)            |


> ⚠️ В данной системе применяется **LSB = 20 пс** (REFCLK\_DIVISIONS = 3125), как компромисс между объёмом данных и разрешающей способностью.

### 3.3 Настройка режимов GPX2 через SPI

Микросхема **TDC-GPX2** управляется через интерфейс **SPI**, используя 6-битную адресацию регистров. Первый байт команды содержит:

* **Бит 7** — направление: `1` = запись, `0` = чтение
* **Биты 5–0** — адрес целевого регистра

#### **Инициализация SPI на Arduino Due:**

```cpp
SPI.begin();
SPI.beginTransaction(SPISettings(20000000, MSBFIRST, SPI_MODE0));  // до 20 МГц
digitalWrite(CS_PIN, LOW);
SPI.transfer16(команда);
digitalWrite(CS_PIN, HIGH);
SPI.endTransaction();
```

#### **Основные регистры конфигурации (выдержка):**

| Название регистра     | Адрес | Описание                                  |
| --------------------- | ----- | ----------------------------------------- |
| `CONFIGURATION`       | 0x00  | Настройка режимов измерения               |
| `REFCLK_DIVISIONS`    | 0x03  | Делитель периода REFCLK для установки LSB |
| `MEASUREMENT_TRIGGER` | 0x05  | Запуск нового измерения                   |
| `READOUT_READY`       | 0x1F  | Флаг готовности результата                |
| `STOP_RESULT1` … `16` | 0x10+ | Результаты измерений STOP-событий         |

> 📌 Адреса регистров для чтения результатов (например, 0x10, 0x11 и далее) зависят от количества каналов и событий. Формат хранения описан в главе Coding of Results даташита GPX2.

#### 📦 Пример конфигурации (Arduino Due, 4 канала, 8 STOP, LSB = 20 пс @ 16 МГц)

```cpp
#include <SPI.h>

#define GPX2_CS   10
#define GPX2_ADDR_WRITE  0x80
#define GPX2_ADDR_READ   0x00
#define GPX2_INT_PIN 2  // Пин, к которому подключён выход INTERRUPT GPX2

void writeGPX2Register(uint8_t address, uint8_t value) {
  digitalWrite(GPX2_CS, LOW);
  SPI.transfer(GPX2_ADDR_WRITE | (address & 0x3F));
  SPI.transfer(value);
  digitalWrite(GPX2_CS, HIGH);
}

uint8_t readGPX2Register(uint8_t address) {
  digitalWrite(GPX2_CS, LOW);
  SPI.transfer(GPX2_ADDR_READ | (address & 0x3F));
  uint8_t result = SPI.transfer(0x00);
  digitalWrite(GPX2_CS, HIGH);
  return result;
}

void setup() {
  pinMode(GPX2_CS, OUTPUT);
  digitalWrite(GPX2_CS, HIGH);
  pinMode(GPX2_INT_PIN, INPUT);

  SPI.begin();
  SPI.beginTransaction(SPISettings(20000000, MSBFIRST, SPI_MODE0)); // до 20 МГц

  // REG0 — активация каналов STOP1P/N – STOP4P/N (биты 0–3)
  writeGPX2Register(0x00, 0b00001111);

  // REG1 — режим 8 STOP-событий + включение REFCLK
  writeGPX2Register(0x01, 0b10001111);

  // REG2 — дополнительные опции (по умолчанию отключены)
  writeGPX2Register(0x02, 0x00);

  // REG3–REG4 — REFCLK_DIVISIONS = 3125 → LSB = 20 пс (для 16 МГц)
  writeGPX2Register(0x03, 0x0C); // MSB (0x0C)
  writeGPX2Register(0x04, 0x35); // LSB (0x35)

  SPI.endTransaction();

  // Далее по внешнему событию запускаем START
}

void loop() {
  // Ожидаем окончания измерения по пину INTERRUPT
  if (digitalRead(GPX2_INT_PIN) == HIGH) {
    SPI.beginTransaction(SPISettings(20000000, MSBFIRST, SPI_MODE0));

    // Пример: чтение регистра 0x10 (первые данные STOP)
    uint8_t msb = readGPX2Register(0x10);
    uint8_t lsb = readGPX2Register(0x11);
    uint16_t result = (msb << 8) | lsb;

    SPI.endTransaction();

    // Обработка результата (в наносекундах, пс и т.д.)
    Serial.print("STOP1 = ");
    Serial.println(result); // Массив регистров зависит от количества событий
  }
}
```
#### 📌 Примечания:

* `REG0` (0x00) — активирует все 4 канала.
* `REG1` (0x01) — включает STOP-события и референсный тактовый сигнал.
* `REFCLK_DIVISIONS = 3125` (регистры 0x03, 0x04) соответствует **LSB = 20 пс** при 16 МГц.
* Для получения результатов требуется опрос соответствующих регистров времени.

---


## 4. Диапазон измерений

### 4.1 Минимальное измеряемое расстояние

Минимальное расстояние зависит от длительности пачки (3–8 импульсов по 40 кГц), системных задержек и минимального интервала между START и первым STOP-событием.

$$
d_{\text{мин}} = (t_{\text{пачка}} + t_{\text{задержка}}) \cdot v
$$

| Кол-во импульсов | Длительность пачки (мкс) | Задержка (мкс) | Минимальное расстояние (мм) |
| ---------------- | ------------------------ | -------------- | --------------------------- |
| 3                | 75                       | 5              | ≈27.6                       |
| 4                | 100                      | 5              | ≈36.1                       |
| 5                | 125                      | 5              | ≈44.6                       |
| 6                | 150                      | 5              | ≈53.2                       |
| 7                | 175                      | 5              | ≈61.7                       |
| 8                | 200                      | 5              | ≈70.2                       |

> Можно уменьшать длительность пачки (3 импульса), чтобы измерять расстояния от **30 мм**.

### 4.2 Максимальное измеряемое расстояние

Максимальный временной интервал, который может зафиксировать TDC-GPX2, достигает **16 секунд**, что даёт огромный диапазон для измерений. В контексте **ультразвукового позиционирования в воздухе** (скорость звука ≈ 343 м/с) это соответствует:

$$
d_{\text{макс}} = v \cdot t_{\text{макс}} = 343 \cdot 16 = 5488\ \text{м}
$$

> На практике диапазон ограничивается не микросхемой, а архитектурой запуска/обработки, временем затухания пачки и частотой повторения измерений (см. п.5.6)

---

## 5. Работа с пачкой ультразвуковых импульсов

Во время измерения микроконтроллер (**Arduino Due**) формирует **пачку из 3–6 импульсов частотой 40 кГц**, которые возбуждают ультразвуковой излучатель (**MA40S4S**) через драйвер L9110. Сигналы распространяются в воздухе и улавливаются приёмниками (**MA40S4R**), далее усиливаются, проходят через компаратор и поступают на логическую часть с D-триггером и одновибраторами, а затем — на микросхемы TDC7200.

### 🧭 5.1. Зачем используется пачка, а не одиночный импульс?

* ✅ **Надёжное детектирование первого фронта.** Один импульс может быть слабым — пачка повышает шанс фиксации начала сигнала (ToF).
* 🔁 **Подтверждение структуры сигнала.** Первый и последующие импульсы позволяют отличить настоящий сигнал от шумов и переотражений.
* 📐 **Анализ фазы.** Сравнение фронта и спада импульсов позволяет восстановить максимум и фазу прихода сигнала.
* 🧠 **Автоматическая адаптация.** По числу зафиксированных импульсов и интервалам между ними система может корректировать усиление (способ возбуждения) и порог срабатывания.

### 🌀 5.2. Инерционный «хвост» сигнала

После окончания возбуждения ультразвукового излучателя пьезоэлемент продолжает колебаться по инерции. Это приводит к появлению затухающих последующих колебаний, называемых «хвостом» сигнала. Хвост представляет собой серию затухающих импульсов с той же частотой (обычно 40 кГц), но уменьшающейся амплитудой и разной формой.

При низком пороге компаратора возможно ложное срабатывание. При ограничении амплитуды усилителя (например, до 3.3 В), хвост может усиливаться до уровня, сравнимого с основной пачкой, несмотря на его меньшую реальную амплитуду. Поэтому надёжная фильтрация хвоста должна учитывать временные интервалы и характер фронтов.. Они возникают с задержкой и могут приводить к ложным срабатываниям компаратора, особенно если порог его срабатывания установлен слишком низко.

Интервал между колебаниями хвоста, как правило, близок к периоду основной частоты (25 мкс при 40 кГц), но амплитуда быстро снижается. Амплитуда последнего сигнала в пачке обычно сопоставима с соседними импульсами и не отличается существенно от них, и следующий за ним сигнал хвоста может быть в 2–3 раза слабее по амплитуде. При этом его фронты могут быть достаточно резкими, чтобы быть зафиксированными компаратором.

Если в пачке задано 5 фронтов, а фиксируется 6 STOP-событий, то дополнительный может быть вызван именно хвостом. Это используется для контроля правильности порога и оценки наличия паразитных импульсов вне основной пачки.

### 🔎 5.3. Анализ пачки и адаптация

**Основная цель анализа** — надёжно зафиксировать **первый фронт первого импульса** (для измерения ToF) и, используя фронты и спады последующих импульсов, **выполнить фазовый анализ** для повышения точности расчёта времени прихода сигнала.

Для этого система регистрирует:

| Параметр                       | Назначение                                        |
| ------------------------------ | ------------------------------------------------- |
| Время первого фронта           | Основной ToF — измерение времени пролёта          |
| Количество STOP-сигналов       | Оценка полноты пачки и устойчивости приёма        |
| Интервалы между фронтами       | Подтверждение структуры пачки                     |
| Разница между фронтом и спадом | Расчёт фазы, определение положения начала сигнала |

> Данные анализа используются для корректировки порога компаратора, адаптации усиления и уточнения момента прихода сигнала.

### ✅ 5.4. Адаптация порога компаратора

При пачке из **5 сигналов**:

### 📊 Количество STOP-событий на канал, например, при 5 импульсах в пачке

| Зарегистрировано STOP | Интерпретация                                                          | Рекомендованные действия                         |
| --------------------- | ---------------------------------------------------------------------- | ------------------------------------------------ |
| 0                     | Сигнал не прошёл через порог вообще                                    | ➤ Снизить порог, усилить сигнал (антифазное возбуждение) |
| 1                     | Только один сигнал — вероятно, пиковое срабатывание            | ➤ Снизить порог, усилить сигнал (антифазное возбуждение)     |
| 2-3                    | Слабое срабатывание, неустойчивый приём                                | ➤ Повторить, возможно усилить сигнал (антифазное возбуждение) |
| 4                     | Один из импульсов не зафиксирован                  | ➤ Проверить стабильность, адаптировать порог     |
| 5                     | Все 4 импульса (2–5) зафиксированы по фронтам/спадам                   | ➤ Порог выбран оптимально                        |
| 6-8                   | 1-3 STOP-события зафиксировано от **инерционного хвоста** (6-8 паразитный пик) | ➤ повысить порог компаратора или ослабить сигнал (однотактное возбуждение) |


Ниже представлены возможные сценарии срабатывания компаратора в зависимости от порога (слишком высокий, высокий, средний, низкий).

![Group 23](https://github.com/user-attachments/assets/55f28c5b-5135-430c-a096-4aa42ca89c94)
*Рис. 2a. Порог срабатывания компаратора слишком высокий*

---

![Group 24](https://github.com/user-attachments/assets/f227a732-4402-40ec-b50d-6b48f21e8dea)
*Рис. 2б. Порог срабатывания компаратора высокий*

---

![Group 25](https://github.com/user-attachments/assets/f3f0dbab-70a3-4a04-aa68-58ba6897076e)
*Рис. 2в. Порог срабатывания компаратора средний*

---

![Group 26](https://github.com/user-attachments/assets/8839a91a-d236-4d4c-84f2-9f9ddb96fcec)
*Рис. 2г. Порог срабатывания компаратора низкий*

### 📐 5.4. Расчёт времени прихода с фазовой поправкой

Чтобы уточнить момент прихода ультразвуковой волны, используется фазовый анализ импульсов в пачке.

Сначала фиксируется:

* `t_{ToF}` — время прихода **первого фронта первого импульса**  
* `t_{rise}[i]`, `t_{fall}[i]` — фронт и спад **i-го импульса**, где `i = 1…N`  
* `T` — период ультразвуковой волны (например, 25 мкс при 40 кГц)

#### 📈 Расчёт времени `t_echo[i]` для каждого импульса

Для каждого импульса рассчитывается момент максимума амплитуды:

```math
t_{\text{avg}[i]} = \frac{t_{\text{rise}[i]} + t_{\text{fall}[i]}}{2}
```

Так как импульс `i` отстоит от первого на **(i−1)** полных периодов, а максимум задержан на **+T/4**, фазово-смещённое время прихода:

```math
t_{\text{echo}[i]} = t_{\text{ToF}} + t_{\text{avg}[i]} - \left( (i - 1) + \frac{1}{4} \right) \cdot T
```

#### 📊 Усреднение `t_echo[i]` по всей пачке

Если все значения `t_{\text{echo}[i]}` получены, можно вычислить среднее арифметическое:

```math
t_{\text{echo}} = \frac{1}{N} \sum_{i = 1}^{N} t_{\text{echo}[i]}
```

#### 🧠 Фильтрация выбросов и адаптивное усреднение

1. Вычисляется промежуточное среднее t̄ₑₜₒ
2. Вычисляется отклонение каждого значения:

```math
\Delta_i = \left| t_{\text{echo}[i]} - \bar{t}_{\text{echo}} \right|
```

3. Фильтруются значения с Δᵢ < Δₘₐₓ (например, < 50 нс)
4. Окончательное значение:

```math
t_{\text{echo}} = \frac{1}{K} \sum_{i \in \text{допустимые}} t_{\text{echo}[i]}
```

> 📌 Такая коррекция и фильтрация позволяют повысить точность даже при наличии хвостов, шумов.

![Group 52](https://github.com/user-attachments/assets/690c06b4-f73a-4181-b32a-08ba38c7e178)  
*Рис. 3. Временная диаграмма измерения времени прихода сигнала с фазовой компенсацией*


### 🎛 5.6. Интервалы между пачками

Чтобы избежать наложения от отражённых волн, необходимо вводить паузы между пачками:

* Максимальное время пролёта (до 3–5 м): ~9–14.5 мс
* Рекомендуемая пауза: **20–30 мс**
* Частота измерений: **30–50 Гц**, что достаточно для ручного перемещения

> Максимальное время (расстояние) пролёта можно увеличить тогда и частоту измерений следует уменьшить.

### ⚙️ 5.7. Алгоритм работы

1. Сформировать пачку из 3–8 импульсов
2. Подать START на GPX2 (все активные каналы)
3. Зафиксировать STOP-сигналы по фронтам и спадам
4. Вычислить `t_avg`, `t_echo`
5. Проверить число STOP, интервалов, стабильность
6. При необходимости — адаптировать порог и усиление
   
### ✅ 5.8. Результаты

* Используется фазовый анализ по фронту/спаду
* Учитывается хвост и количество срабатываний
* Расчёт `t_echo` корректируется по фазе
* Адаптация усиливает устойчивость при слабых сигналах
* Поддерживается работа на дальности до 5 м и более



---

## 5. Получение и анализ данных с TDC7200 (Arduino Due)

После завершения измерения микроконтроллер Arduino Due считывает временные метки с микросхем TDC7200. В системе используется три микросхемы:
- **TDC #1** — режим 2, измерение ToF (время пролёта);
- **TDC #2** — режим 1, регистрация фронтов;
- **TDC #3** — режим 1, регистрация спадов.

### 📥 5.1. Считывание данных с TDC7200

Каждая микросхема имеет регистры, в которых хранится значение времени:
- `TIME1` — первая временная метка STOP после START;
- `TIME2`, `TIME3`, `TIME4`, `TIME5` — дополнительные метки (режим 1).

Пример функции чтения `TIME1` по SPI:
```cpp
uint32_t readTDC_TIME1(uint8_t csPin) {
  uint32_t time = 0;
  digitalWrite(csPin, LOW);
  SPI.transfer(0x10); // TIME1 address
  time |= (uint32_t)SPI.transfer(0x00) << 16;
  time |= (uint32_t)SPI.transfer(0x00) << 8;
  time |= (uint32_t)SPI.transfer(0x00);
  digitalWrite(csPin, HIGH);
  return time;
}
```

Для режимов:
- **Режим 2 (TDC #1)**: разрешение = **55 пс**, только `TIME1`;
- **Режим 1 (TDC #2 и #3)**: разрешение = **125 пс**, до 5 STOP меток.

### 📊 5.2. Пример обработки всех TDC
```cpp
uint32_t tdc_times[6] = {0};

// Чтение TIME1 (ToF) от TDC1 (режим 2)
tdc_times[0] = readTDC_TIME1(TDC1_CS_PIN);
float LSB_ps_TDC1 = 55.0; // Можно использовать фиксированное, так как режим 2
float tof_ns = tdc_times[0] * LSB_ps_TDC1;

// ==== TDC2: режим 1 ====
// Калибровка
uint32_t cal1_TDC2 = readTDC_CAL1(TDC2_CS_PIN);
uint32_t cal2_TDC2 = readTDC_CAL2(TDC2_CS_PIN);
float LSB_ps_TDC2 = (2.0 * 125.0) / (cal2_TDC2 - cal1_TDC2); // при 8 МГц

tdc_times[1] = readTDC_TIME1(TDC2_CS_PIN);
tdc_times[2] = readTDC_TIME2(TDC2_CS_PIN);
float t_rise_ns = tdc_times[1] * LSB_ps_TDC2;
float t_rise2_ns = tdc_times[2] * LSB_ps_TDC2;

// ==== TDC3: режим 1 ====
uint32_t cal1_TDC3 = readTDC_CAL1(TDC3_CS_PIN);
uint32_t cal2_TDC3 = readTDC_CAL2(TDC3_CS_PIN);
float LSB_ps_TDC3 = (2.0 * 125.0) / (cal2_TDC3 - cal1_TDC3);

tdc_times[3] = readTDC_TIME1(TDC3_CS_PIN);
tdc_times[4] = readTDC_TIME2(TDC3_CS_PIN);
float t_fall_ns = tdc_times[3] * LSB_ps_TDC3;
float t_fall2_ns = tdc_times[4] * LSB_ps_TDC3;
```

## 🧠 5.3. Расчёт времени прихода сигнала

После получения измерений от всех трёх микросхем TDC7200 выполняется расчёт времени прихода ультразвуковой волны с учётом фазовой поправки.

### 📏 ToF (время пролёта от излучателя до приёмника)

Измеряется первой микросхемой **TDC #1** (режим 2) по первому фронту первого импульса:

```cpp
float t_us = tof_ns / 1000.0;
float distance_m = (t_us * speed_of_sound) / 1e6; // Скорость звука в м/с
```

Значение `tof_ns` рассчитывается из регистра TIME1:

```cpp
float tof_ns = tdc_times[0] * LSB_ps_TDC1; // TDC #1, LSB ≈ 55 пс
```

---

### 📐 Фазовая поправка (на основе второго импульса)

Второй и последующие импульсы пачки анализируются TDC #2 и TDC #3 (режим 1):

```cpp
float t_rise_ns = tdc_times[1] * LSB_ps_TDC2; // TDC #2 — фронт второго импульса
float t_fall_ns = tdc_times[3] * LSB_ps_TDC3; // TDC #3 — спад второго импульса
float t_avg = (t_rise_ns + t_fall_ns) / 2.0; // Максимум (90°)
```

С учётом того, что второй импульс следует на 1 период позже и его максимум сдвинут на 90°, итоговое время прихода рассчитывается как:

```cpp
float t_echo = tof_ns + t_avg - 31250.0; // T = 25 мкс, T + T/4 = 31.25 мкс
```

> Такая формула позволяет компенсировать фазовый сдвиг и восстановить время прихода ультразвуковой волны с высокой точностью даже при использовании второго импульса для анализа.

---

### 🔎 Примечание

Значения в массиве `tdc_times[]` считываются по SPI при наличии STOP-сигнала:

```cpp
// Примерная структура массива:
// tdc_times[0] — TDC1, ToF (режим 2)
// tdc_times[1], [2] — TDC2, TIME1, TIME2 (фронты)
// tdc_times[3], [4] — TDC3, TIME1, TIME2 (спады)
```
Контроллер самостоятельно определяет, какие значения использовать, в зависимости от режима работы и числа зарегистрированных STOP.

### 🧩 5.4. Интерпретация данных
- Если `tof_ns` в пределах ожидаемого расстояния — фронт принят корректно.
- Если `tdc_times[1]`, `tdc_times[3]` сильно отличаются — возможно фазовый сдвиг.
- Если получено меньше 4 значений STOP от TDC #2 и #3 — возможен пропуск.

### ✅ 5.5. Преобразование в миллиметры
```cpp
float distance_mm = distance_m * 1000.0;
```

Это значение можно сохранить, визуализировать или использовать для построения координатной карты.

### 💾 5.6. Хранение и отправка данных
После получения временных значений система может:
- сохранить данные в массив;
- отправить по USB/UART;
- отобразить координаты преобразователя в приложении или логгере.

```cpp
struct Measurement {
  float tof_ns;
  float t_rise_ns;
  float t_fall_ns;
  float distance_mm;
  uint8_t pulse_count;
};
```

---


### 6. Определение скорости звука в воздухе

Для точного расчёта координат требуется учитывать скорость звука, зависящую от условий окружающей среды. Она определяется по температуре и влажности воздуха:

**Формула с учётом температуры и влажности:**
`v = 331.3 + 0.606·T + 0.0124·RH`

Где:

* `T` — температура (°C)
* `RH` — относительная влажность (%)

**Рекомендуемые датчики для Arduino Due:**

| Параметр           | Датчик        | Интерфейс | Примечания                                    |
| ------------------ | ------------- | --------- | --------------------------------------------- |
| Температура        | DS18B20       | 1-Wire    | Просто, стабильно, ±0.5 °C                    |
| Темп. + влажн.     | SHT31 / SHT35 | I²C       | Точные, быстрые, компактные                   |
| Темп./влажн./давл. | BME280        | I²C / SPI | Универсальный, подходит для расширенных задач |

**Пример кода для SHT31:**

```cpp
#include <Wire.h>
#include "Adafruit_SHT31.h"
Adafruit_SHT31 sht31 = Adafruit_SHT31();

void setup() {
  Serial.begin(9600);
  if (!sht31.begin(0x44)) {
    Serial.println("SHT31 not found");
    while (1) delay(1);
  }
}

void loop() {
  float temperature = sht31.readTemperature();
  float humidity = sht31.readHumidity();
  float v = 331.3 + 0.606 * temperature + 0.0124 * humidity;
  Serial.print("Speed of sound: ");
  Serial.print(v);
  Serial.println(" m/s");
  delay(1000);
}
```

---

## 7. Расчёт разрешающей способности системы

Система на базе микросхемы **TDC-GPX2** и логики фиксации фронтов и спадов сигналов позволяет достигать высокой временной и пространственной точности при регистрации ультразвуковых импульсов.

### 🎯 Основные определения

* **Разрешающая способность** — минимальное изменение времени/расстояния, которое система способна надёжно различить.
* Величина напрямую связана с **LSB (наименьшим значащим битом)** таймера микросхемы TDC-GPX2 и общей точностью измерений.

### ⏱️ Временная разрешающая способность

| Параметр                  | Значение  | Комментарий                                      |
| ------------------------- | --------- | ------------------------------------------------ |
| LSB (режим измерения TOF) | **10 пс** | Наименьший интервал, различимый микросхемой GPX2 |

### 📏 Пространственная разрешающая способность

На воздухе скорость звука ≈ **343 м/с**:

```text
343 м/с → 0.343 мм/мкс → 0.343 нм/нс → 0.343 пм/пс
```

Исходя из этого:

* При 10 пс: **0.343 × 10 = 3.43 нм**

> 📌 Это теоретически возможное разрешение. Фактическая точность зависит от других факторов.

### ⚠️ Реальные ограничения (ошибки и погрешности)

| Источник погрешности               | Оценка (примерно) | Комментарий                                                                |
| ---------------------------------- | ----------------- | -------------------------------------------------------------------------- |
| Джиттер компаратора (TLV3501)      | ±100–200 пс       | Отклонение времени срабатывания на фронте                                  |
| Погрешность DAC и дрейф порога     | ±100 пс           | Смещение порога изменяет время перехода сигнала через него                 |
| Длительность формируемого импульса | \~100–150 нс      | Определяет минимальный интервал между фронтами, может ограничивать частоту |
| Шумы и флуктуации сигнала на входе | ±50–200 пс        | Особенно значимо при малой амплитуде и близком к порогу уровне сигнала     |

> 🛈 **Джиттер** — случайное отклонение времени срабатывания компаратора.
> 🛈 **Флуктуации порога** — небольшие изменения порогового уровня, вызываемые нестабильностью DAC или питающей цепи.

### 📌 Вывод:

* Теоретически, система способна регистрировать перемещения с разрешением **менее 5 нм**.
* Практически, с учётом всех аппаратных ограничений, **разрешающая способность составляет ≈250–500 нс по времени** (что соответствует \~85–170 мкм по расстоянию).
* Это обеспечивает достаточную точность для задач **позиционирования ультразвукового преобразователя в пространстве**.



## 7. Применения в контроле и позиционировании

### 7.1. Один канал — позиционирование системы TOFD или УЗ-сканера
Один канал может быть использован для определения положения мобильной измерительной системы, такой как:

- система **TOFD** (Time-of-Flight Diffraction);
- **ручной ультразвуковой сканер на фазированных решётках**.

В этом случае при каждом измерении фиксируются координаты положения устройства. Это даёт возможность для **картографирования результатов** вдоль шва или в каждом сечении сварного соединения.

> Такой подход позволяет пространственно соотнести измеренные данные и точно отображать их на карте обследуемой области.

### 7.2. Два канала — определение положения на плоскости
При использовании двух приёмников, расположенных по разные стороны сварного шва, можно определить координату ультразвукового преобразователя относительно центра шва.

Методы расчёта включают:
- **пересечение окружностей** по известным расстояниям;
- **триангуляцию** по задержкам сигнала;
- **пересечение гипербол** (для более высокой точности и устойчивости в условиях шума).

Также возможна оценка толщины с разных сторон от шва с использованием геометрического анализа. Для этого два или четыре приёмника могут фиксировать время прихода сигнала от излучателя, установленного на преобразователе, и определять его координаты с высокой точностью.

### 7.3. 6–8 каналов — позиционирование вокруг окружности трубы
При контроле сварных соединений труб необходимо знать положение преобразователя на цилиндрической поверхности.

Расположив 6–8 приёмников вокруг окружности, можно определить координаты излучателя в **цилиндрической системе координат**, используя разность времён прихода сигнала на каждый из приёмников.

> Это особенно актуально при полном сканировании труб или сосудов под давлением, где требуется контроль по всей окружности.

#### 7.4. Сводная таблица применения

| Кол-во каналов | Применение                                                             | Особенности                                                   |
|----------------|------------------------------------------------------------------------|----------------------------------------------------------------|
| 1              | TOFD, фазированные решётки                                             | Без позиционирования, фиксация времени прихода                |
| 2              | Позиционирование по оси шва, измерение толщины                         | Триангуляция или пересечение радиусов дальности               |
| 3–4            | Уточнение координат, фильтрация шумов                                  | Пересечение гипербол, повышенная устойчивость                 |
| 6–8            | Позиционирование по окружности (например, трубы)                       | Цилиндрическая система координат, полное покрытие             |

---


## 8. Алгоритмы и логика работы

Система построена на принципе автоматического запуска измерений с помощью сигнала `INTB` от TDC7200. Это позволяет достигать высокой частоты измерений без программных задержек. Ниже представлена сводная таблица с описанием ключевых этапов логики работы:

| Этап                         | Описание                                                                                   |
|-----------------------------|--------------------------------------------------------------------------------------------|
| 1. Формирование START       | Первый `START` подаётся от Arduino; далее новые `START` инициируются по `INTB` (через логику AND+OR). |
| 2. Подача пачки импульсов   | Генерация от 3 до 12 импульсов 40 кГц; длительность и количество адаптируются под условия. |
| 3. Приём сигнала            | Компаратор фиксирует фронт сигнала и подаёт `STOP` в TDC7200.                              |
| 4. Анализ интервалов        | Arduino сравнивает интервалы между импульсами: >50 мкс → конец цикла.                      |
| 5. Подсчёт импульсов        | Если получено 10/10 импульсов — данные точные; <10 — возможна компенсация на 25 мкс.       |
| 6. Проверка на "хвост"      | Анализ отклонений между интервалами, например, <22–23 мкс → фронт может быть поздним.      |
| 7. Адаптация порога         | Порог компаратора корректируется по стабильности числа импульсов и интервалов.            |
| 8. Расчёт координат         | По времени `STOP` и известной скорости звука вычисляется расстояние/позиция.              |
| 9. Сохранение/отправка      | Данные сохраняются и/или передаются в систему анализа/визуализации.                       |

![Group 5 (2)](https://github.com/user-attachments/assets/b3bea198-90d9-4f54-92f3-9d198e9c4c13)


> 💡 Промежуточные START-импульсы не зависят от кода Arduino и формируются логически, обеспечивая минимальную задержку.

---


## 9. Преимущества системы

✅ Предложенная система позиционирования ультразвукового преобразователя на базе TDC7200 проектируется с учетом следующих технических и функциональных преимуществ:

### 🎯 9.1. Высокая точность
- Разрешение до **55 пикосекунд** благодаря TDC7200, должна обеспечить высокую точность


### 🚀 9.2. Высокая частота измерений
- Аппаратный автозапуск через **INTB** без участия CPU.
- Частота повторения измерений — **40–65Гц** что достаточно для плавного ручного сканирования.

### 🧠 9.3. Адаптивное управление чувствительностью
- Порог срабатывания компаратора регулируется **DAC** в реальном времени.
- Подстраивается в зависимости от количества принятых импульсов и интервалов между ними.

### 🎛 9.4. Управление длиной пачки
- Контроллер адаптирует количество возбуждающих импульсов:
  - **Короткие расстояния** → меньше импульсов (например, 3–5) ⇒ меньше мёртвая зона.
  - **Большие расстояния** → больше импульсов (например, 8–12) ⇒ лучше обнаружение.
- Возможна предварительная серия **пробных** измерений для автонастройки.

### 🌡 9.5. Учет условий среды
- Подключение датчика **температуры и влажности** (например, SHT31) по **I²C**.
- Расчёт **скорости звука с учётом окружающей среды** ⇒ более точное позиционирование.

### 🔧 9.6. Простота масштабирования
- Архитектура легко расширяется от **1 до 8 и более каналов**.
- Одинаковая логика работы и повторяемость схемы.

### 🧩 9.7. Гибкость интеграции
- Подходит для различных методов контроля:
  - TOFD,
  - ультразвуковые сканеры на фазированных решётках,
  - триангуляция и пересечение радиусов дальности и гипербол от разности прихода сигнала по различным приемникам,
  - позиционирование по окружности.
- Может использоваться как **самостоятельная подсистема позиционирования** в составе более крупного комплекса НК.

---


## 10. Ограничения и особенности эксплуатации

⚠️ Несмотря на преимущества, предлагаемая архитектура системы имеет потенциальные ограничения, которые необходимо учитывать при проектировании и использовании.

### 📏 10.1. Ограниченный диапазон измерений
- **Максимальное расстояние** ограничено временем измерения в режиме 2 (до 8 мс), что соответствует примерно **2.7 м** в воздухе.
- **Минимальное расстояние** зависит от длительности пачки и системных задержек:
  - При 10 импульсах ~225–250 мкс ⇒ минимальное расстояние ≈ **80 мм**.
  - При 5 импульсах ⇒ можно снизить до **40 мм**.
- При слишком малом расстоянии первый импульс может попасть в «мёртвую зону» и не зафиксироваться, а при превышении 8 мс приём будет проигнорирован.
- Требуется **адаптация количества импульсов** в зависимости от расстояния.

### 🔁 10.2. Необходимость калибровки
- Скорость звука может варьироваться в зависимости от температуры, влажности, атмосферного давления.
- Даже при наличии датчиков окружающей среды, **требуется начальная калибровка**:
  - Установка начальной точки,
  - Сравнение с эталонной длиной,
  - Коррекция расчётов на практике.

### 📐 10.3. Геометрическая привязка и выбор системы координат
- Для точного позиционирования необходима **геометрическая модель**:
  - Прямая линия (1D),
  - Плоскость (2D),
  - Цилиндрическая система (например, труба),
- Также важно учесть начальные смещения: система должна знать, где находится **нулевая точка** и направление координат.
- Алгоритмы расчёта различаются для гипербол, триангуляции или радиусов.

### 🧭 10.4. Позиционирование приёмников
- Расстояния между приёмниками должны быть **известны с высокой точностью** (например, 0.5 мм).
- Приёмники должны быть установлены **жёстко и стабильно**, исключая сдвиги и вибрации.
- При изменении конфигурации системы требуется повторная калибровка.

### 🔍 10.5. Прямая видимость между излучателем и приёмниками
- Ультразвуковые волны (40 кГц) распространяются **по прямой линии**.
- Между излучателем и приёмником не должно быть:
  - Частей тела оператора,
  - Посторонних предметов,
  - Краёв конструкции (например, труб, стен).
- Угол излучения должен перекрывать зону чувствительности приёмников.

### 🌀 10.6. Влияние условий эксплуатации
- Несмотря на адаптацию порога и фильтрацию, сильные **шумы, вибрации**, резкие удары по конструкции могут исказить сигнал.
- Приёмники, установленные на вибрирующих трубах, требуют демпфирования или компенсации в алгоритме.
- Также важно учитывать **акустические переотражения** от стен, сварных усилений, швов.

### 🌪 10.7. Влияние внешней среды
- **Ветер** может отклонять акустическую волну, особенно на расстояниях >1.5 м.
- **Осадки (дождь, снег)** создают акустические помехи и затухание.
- Рекомендуется:
  - Работа в защищённых условиях,
  - Применение экранирующих кожухов,
  - Автоматическая оценка надёжности измерения по качеству пачки.

---


## 11. Заключение

Предложенная система на базе микросхемы **TDC7200** реализует **иной подход** к определению координат при ручном неразрушающем контроле. В отличие от традиционных решений на базе механических энкодеров или анализа отражённого сигнала, предложенная система использует **внешние ультразвуковые приёмники и излучатель**, работающие в воздушной среде. Благодаря этому позиционирование может происходить **независимо от основной измерительной системы**, работающей в диапазоне 1–15 МГц.

Система построена на расчётах и принципах работы микросхемы TDC7200, адаптивной логики запуска, управления порогом срабатывания компаратора и учёта условий среды. Теоретически она способна обеспечивать определение положения преобразователя в пределах до 2.7 м с точностью до миллиметров. Однако, как и в случае с любой экспериментальной разработки, **реальная точность и устойчивость системы будут определяться условиями эксплуатации и должны быть подтверждены практическими испытаниями**.

Несмотря на возможные ограничения и «подводные камни», описанная архитектура демонстрирует потенциал для создания **модульной, расширяемой и малозависимой от механики системы позиционирования**. Это может существенно повысить информативность УЗ-контроля, особенно при ручном сканировании, и открыть путь к интеграции позиционирования в существующие средства НК без значительной переделки аппаратной части. 

---

## Связанные материалы

- [🛠 Реализация аппаратной части системы](https://github.com/du-dim/Ultrasonic-Emitter-Positioning-System/blob/main/hardware.md)

---

## Автор

**Дмитрий Дубовик**✍   
Инженер по неразрушающему контролю  
**Дата:** 17.05.2025  
**Email/контакт:** [dzm.dubovik@gmail.com]  
**GitHub:** [@du-dim](https://github.com/du-dim)

*Подготовлено как технический обзор и авторский проект.*  
*Использование возможно в рамках производственных и исследовательских задач.*  
*Проект находится в развитии — на этапе проектирования электроники и печатных плат.*

