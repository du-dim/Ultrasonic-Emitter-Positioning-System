# Система позиционирования ультразвукового излучателя на базе TDC7200 с адаптивной логикой и управлением порогом 



## 📌 Аннотация
Ультразвуковой контроль (UT) — метод неразрушающего контроля, широко применяемый в промышленности, особенно для контроля сварных соединений и измерения толщины. Одним из ключевых ограничений традиционного ультразвукового контроля с наклонным преобразователем, особенно при ручном сканировании является отсутствие локализации или точной локализации положения пьезоэлектрического преобразователя (ПЭП) в пространстве. Для решения этой задачи предложена архитектура системы позиционирования ПЭП, позволяющая регистрировать его координаты при каждом измерении.

---

## 📑 Содержание

1. [Введение](#1-введение)
2. [Микроконтроллер и архитектура системы](#2-микроконтроллер-и-архитектура-системы)
3. [Режим работы TDC7200 и диапазон измерений](#3-режим-работы-tdc7200-и-диапазон-измерений)  
4. [Работа с пачкой ультразвуковых сигналов](#4-работа-с-пачкой-ультразвуковых-сигналов)  
5. [Сохранение и анализ данных с TDC7200](#5-сохранение-и-анализ-данных-с-tdc7200)
6. [Определение скорости звука в воздухе](#6-определение-скорости-звука-в-воздухе)
7. [Применения в контроле и позиционировании](#7-применения-в-контроле-и-позиционировании)  
8. [Алгоритмы и логика работы](#8-алгоритмы-и-логика-работы)  
9. [Преимущества системы](#9-преимущества-системы)  
10. [Ограничения и особенности эксплуатации](#10-ограничения-и-особенности-эксплуатации)  
11. [Заключение](#11-заключение) 
    [Связанные материалы](#cвязанные-материалы) 
    [Автор](#автор)

---

## Введение

Позиционирование ультразвукового пьезоэлектрического преобразователя (ПЭП) — одного из ключевых элементов неразрушающего контроля металлов — играет решающую роль при оценке сварных соединений и в других задачах, где важно отличить реальные дефекты от ложных отражений. Такие ПЭП обычно работают в диапазоне **1–15 МГц** и используются для измерения толщины, поиска дефектов и других задач контроля.

Однако при ручном сканировании на плоских и особенно криволинейных поверхностях, положение ПЭП в пространстве неизвестно, что затрудняет интерпретацию результатов. Для решения этой проблемы предложена вспомогательная **система позиционирования ПЭП**, основанная на регистрации ультразвуковых импульсов в диапазоне **40 кГц**.

Система состоит из миниатюрного **излучателя 40 кГц** (например, MA40S4S), прикреплённого к корпусу ПЭП, и **нескольких приёмников 40 кГц** (например, MA40S4R), расположенных вне зоны контроля. При каждом измерении основной системой одновременно формируется пачка сигналов от излучателя. По времени их прихода к приёмникам и известной скорости звука в воздухе система определяет координаты ПЭП.

Аппаратная часть построена на микросхеме **TDC7200**, обеспечивающей разрешение измерения временных интервалов до **55 пикосекунд**, что позволяет с высокой точностью фиксировать задержки между сигналами START и STOP.  
Источник: [TI TDC7200 Datasheet](https://www.ti.com/lit/gpn/TDC7200)

Для расширения возможностей используется **режим 2**, поддерживающий измерения до **8 мс**, что соответствует расстоянию до ~2,7 м в воздухе (в зависимости от температуры и влажности).

Первый импульс `START` формирует микроконтроллер (**Arduino DUE**), последующие импульсы формируются **аппаратно** — на основе сигнала `INTB`, генерируемого после завершения каждого цикла измерения. Этот сигнал проходит через **инвертор** и логический элемент **AND**, а затем объединяется через **OR** с управляющим сигналом от контроллера.

> Такая логика исключает программные задержки и обеспечивает частоту измерений до 16–20 кГц.

Компаратор, определяющий момент прихода сигнала, имеет **адаптивный порог**, настраиваемый через `DAC` контроллера. Порог регулируется в зависимости от условий — количества принятых импульсов и временных интервалов между ними.

Интервалы между импульсами 40 кГц составляют ~25 мкс. Если между `STOP`-сигналами фиксируется интервал более 50 мкс — измерение считается завершённым.

Также в систему интегрирован цифровой датчик **температуры и влажности** (например, **SHT31**) по интерфейсу I²C, позволяющий учитывать влияние атмосферных условий на скорость звука.

> Скорость звука в воздухе зависит не только от температуры, но и от влажности, и влияет на точность позиционирования.

Важно отметить, что **вывод TRIGG** TDC7200 в архитектуре **не используется**. Все циклы управляются через `START` и `INTB`, а использование режима 2 позволяет отказаться от дополнительных временных ограничений, связанных с TRIGG.

![Group 4](https://github.com/user-attachments/assets/ee1d929f-2a0d-4c56-8ec5-981802683fe8)
*Рис. 1. Управляющая архитектура: связи между Arduino Due, TDC7200, компаратором, логикой, датчиком температуры/влажности и УЗ-датчиками 40 кГц*

---


## 1. Микроконтроллер и архитектура системы

Центральное управление системой выполняется микроконтроллером **Arduino Due**, выбранным за счёт высокой тактовой частоты, наличия встроенного ЦАП, достаточного количества цифровых выводов и удобной работы с SPI и I²C-интерфейсами.

### 🔁 Общая структура

Полная архитектура охватывает весь путь сигнала — от генерации ультразвука до его приёма и точного измерения времени прохождения:

```text
Arduino DUE → MOSFET → Излучатель → Воздух → Приемник
→ Усилитель → Компаратор → TDC7200 → SPI → Arduino DUE
```

### 📡 1.1. Ультразвуковой излучатель
Излучатель (например, MA40S4S) генерирует пачку ультразвуковых импульсов частотой **40 кГц**. Эти импульсы формируются на Arduino и усиливаются через ключ на **MOSFET-транзисторе** (например, IRLZ44N), подключённом к источнику питания 12 В. Такое усиление необходимо для обеспечения достаточной акустической мощности сигнала.

- Управление MOSFET осуществляется цифровым выходом Arduino.
- Пачка состоит из 5–10 импульсов длительностью по 12.5 мкс каждый (полпериода 40 кГц).

### 📥 1.2. Приёмник
Приёмник (например, MA40S4R) принимает отражённый или прямой ультразвуковой сигнал. Сигнал с выхода приёмника поступает на многоступенчатый **усилитель на операционных усилителях** (например, OPA134 или OPA4134), предназначенный для работы с однополярным питанием и усиления слабых сигналов (от единиц мВ до сотен мВ).

- Усиление настраивается так, чтобы выходной сигнал оставался в пределах 3.3 В, допустимых для дальнейшей обработки.

### ⚡ 1.3. Компаратор
Усиленный аналоговый сигнал подаётся на вход **высокоскоростного компаратора** (например, TLV3501), порог которого задаётся с **ЦАП Arduino Due** (DAC). Компаратор сравнивает входной сигнал с порогом и генерирует цифровой фронт при каждом превышении порога.

- Цифровой выход компаратора соединён со входом **STOP** микросхемы **TDC7200**.
- Порог может адаптироваться в реальном времени в зависимости от стабильности приёма пачки импульсов.

### ⏱ 1.4. Микросхема TDC7200
Микросхема **TDC7200** производит измерение временного интервала между сигналом **START** и одним или несколькими сигналами **STOP**. Используется **режим 2**, который допускает только один STOP-сигнал на каждый START, но с максимальной длительностью измерения до **8 мс**, позволяя контролировать расстояния до **2.7 м**.

- Первый импульс START формируется Arduino.
- Последующие START-импульсы формируются **аппаратно** — по завершению измерения, на основе сигнала **INTB**.

### 🧠 1.5. Микроконтроллер (Arduino Due)
Функции микроконтроллера:

- **SPI** — конфигурация TDC7200, чтение измерений.
- **DAC** — установка порога компаратора.
- **I²C** — подключение датчика температуры и влажности (например, SHT31).
- **Цифровые выводы** — формирование пачек возбуждающих импульсов, логическое управление START, EN-флаг для включения логики.

Дополнительно реализуется **логика запуска**:

- `INTB` → инвертор → AND + EN → OR → `START`
- Arduino может также подавать START через ту же линию OR.

### 🌡 1.6. Датчик температуры и влажности
Для коррекции скорости звука используется цифровой датчик (например, **SHT31**), подключённый по I²C. Его показания позволяют уточнить расчёт расстояний по известной формуле зависимости скорости звука от температуры и влажности воздуха.

> 🛈 Общая схема архитектуры приведена в конце раздела «Введение» (Рис. 1).

---


## 2. Режим работы TDC7200 и диапазон измерений

### 2.1. Выбор режима TDC7200 и максимальное измеряемое расстояние
Микросхема **TDC7200** поддерживает два режима измерения времени между импульсами START и STOP:

| Режим | Кол-во STOP | Время измерения          | Точность        | Мин. интервал | Применение                                         |
|-------|-------------|---------------------------|------------------|----------------|----------------------------------------------------|
| 1     | до 5        | до 500 нс                 | до 55 пс         | > 12 нс        | Высокоточные измерения коротких интервалов         |
| 2     | только 1    | до 8 мс                   | до 55 пс         | > 250 нс       | Измерения больших расстояний (до 2.7 м в воздухе)  |

Для задач ультразвукового позиционирования в воздухе (например, при 40 кГц) выбран **режим 2**, так как он позволяет отслеживать задержки сигнала до 8 мс — этого достаточно, чтобы зафиксировать пройденный сигнал приемником от излучателя на расстоянии **до ~2.7 м**:

$$
d_{\text{макс}} = v \cdot t_{\text{max}} = 343 \cdot 0.008 = 2.744\ \text{м}
$$


### 2.2 Минимальное измеряемое расстояние
Так как TDC7200 в режиме 2 фиксирует только **первый** сигнал STOP после START, необходимо учитывать длительность пачки и системные задержки, чтобы сигнал не попал внутрь «мертвой зоны».

Формула:

$$
d_{\text{min}} = (t_{\text{пачка}} + t_{\text{задержек}}) \cdot v
$$

| Кол-во импульсов | Длительность пачки (мкс) | Задержка (мкс) | Минимальное расстояние (мм) |
|------------------|--------------------------|----------------|------------------------------|
| 10               | 225                      | 5              | ≈78.3                        |
| 5                | 100                      | 5              | ≈36.2                        |
| 3                | 50                       | 5              | ≈18.8                        |

> Уменьшая количество импульсов, можно снизить минимально измеряемое расстояние до **20 мм**.

### 2.3 Адаптация длины пачки
Для увеличения гибкости и устойчивости измерений, контроллер может адаптировать количество импульсов в пачке в зависимости от предварительно измеренной задержки:

| Задержка    | Импульсы |
| ----------- | -------- |
| < 300 мкс   | 3–5      |
| 300–500 мкс | 6–8      |
| > 500 мкс   | 10–12    |


Также можно реализовать **пробную серию** из 3–5 измерений, не передавая данные далее, чтобы:
- оценить примерное расстояние;
- подобрать оптимальное число импульсов,
- адаптировать порог компаратора,
- задать длительность окна ожидания.

Таким образом, использование режима 2 в сочетании с адаптацией пачки позволяет:
- **расширить диапазон измерений** (до 2.7 м),
- **снизить минимально измеряемое расстояние**,
- **повысить точность за счёт адаптации** к условиям сигнала,
- **исключить перекрытие пачки и первого отражения**.

### 🔧 2.4. Установка режима 2 на Arduino
Чтобы TDC7200 работала в режиме 2 (режим одного STOP и увеличенного времени измерения до 8 мс), необходимо правильно настроить регистр конфигурации CONFIG1 через SPI.

Пример кода на Arduino:

```cpp
#define TDC7200_WRITE_CONFIG1 0x40 // Адрес записи в CONFIG1 (0x00 | 0x40)

void setTDCMode2() {
  digitalWrite(SS, LOW); // Активируем SPI-чипселект
  SPI.transfer(TDC7200_WRITE_CONFIG1); // Указываем, что хотим записывать в CONFIG1

  /*
    CONFIG1 (битовое поле):
    [7] FORCE_CAL (0)
    [6] START_MEAS (0)
    [5:3] MEAS_MODE = 001 => режим 2
    [2:1] RESERVED (0)
    [0] TDC_ENABLE = 1
    Итого: 0b00001001 = 0x09
  */
  SPI.transfer(0x09); // Устанавливаем режим 2
  digitalWrite(SS, HIGH); // Деактивируем SPI
}
```

---


## 3. Работа с пачкой ультразвуковых импульсов

Во время измерения микроконтроллер (**Arduino Due**) формирует **пачку из 3–12 импульсов частотой 40 кГц**, которые возбуждают ультразвуковой излучатель (**MA40S4S**). Сигналы распространяются через воздух и улавливаются приёмниками (**MA40S4R**), подключёнными через усилитель и компаратор к микросхеме **TDC7200**.

### 🧭 3.1 Зачем используется пачка, а не одиночный импульс?
- **Повышение надёжности** регистрации сигнала
- **Фильтрация ложных срабатываний**
- **Оценка достоверности** первого принятых фронтов
- **Возможность компенсации** при пропуске первого импульса

### 🌀 3.2 Инерционный «хвост» сигнала
После приёма акустического сигнала пьезоэлектрический элемент продолжает колебаться по инерции, создавая **затухающую серию импульсов** — «хвост».

| Параметр              | Значение               |
|-----------------------|------------------------|
| Длительность хвоста   | ≈ 100–150 мкс          |
| Интервал между пиками | ≈ 25 мкс (40 кГц)      |
| Первый пик            | Самый точный по времени |
| Последующие пики      | Смещены по фазе/амплитуде |

> Система должна стремиться зафиксировать именно **первый фронт**, так как он наиболее точно отражает момент прихода ультразвуковой волны.

### 🔎 3.3 Анализ принятой пачки и компенсация
Принимаются только **фронты**, прошедшие через компаратор. В режиме 2 TDC7200 фиксируется только **один STOP** — первый зарегистрированный фронт. Контроллер анализирует:

1. **Сколько импульсов принято из всей пачки** (по количеству измерений).
2. **Интервалы между импульсами** (по INTB-фронтам).
3. **Корректность порога компаратора**.

#### Сценарии:

| Принято | Интервалы         | Действие                                                            |
|---------|-------------------|---------------------------------------------------------------------|
| 10/10   | ~25 мкс           | Считаем, что первый импульс принят корректно — `t_echo = STOP - START` |
| 9/10    | ~25 мкс           | Возможен пропуск первого — добавляем +25 мкс                        |
| <9      | нестабильные      | Снижаем порог, повторяем измерение или увеличиваем усиление        |
| 10/10   | 20–23 мкс         | Сработал не по фронту, а по пику — **используем 2-й + 25 мкс**     |
| 10/10   | 17–20 мкс         | Более выраженное смещение — **используем 3-й + 25 мкс** или по ситуации |

### 📐 3.4 Интерпретация «слишком раннего» срабатывания
Если компаратор сработал на пике, а не на фронте, временная метка оказывается **заниженной**. Анализ промежутков между пиками позволяет обнаружить это:

```text
Ожидаемый интервал между пиками при 40 кГц: ≈25 мкс

Если зафиксированы интервалы:
- ~23 мкс → возможно, 1-й импульс — пиковый, не фронтовой
- ~20 мкс → большая вероятность срабатывания на пике
```

> В таких случаях корректнее взять время следующего фронта и добавить к нему 25 мкс — это компенсирует ошибку и сдвигает метку к реальному приходу волны.


### ⚙️ 3.5. Алгоритм работы

1. Сформировать пачку из N импульсов (по адаптированной длине).
2. Зафиксировать 1-й STOP с помощью TDC7200.
3. Оценить количество принятых импульсов.
4. Проанализировать интервалы между INTB (25 мкс — эталон).
5. Принять решение:
   - При 10/10 + интервалы ~25 мкс — OK
   - При <10 или интервалы <24 мкс — применить компенсацию
6. При необходимости — изменить порог компаратора через DAC.


### ⏱ 3.6. Формула расчёта времени прихода

```text
t_echo = t_STOP - t_START

if (1-й импульс пропущен или сработал по пику)
    t_echo_corrected = t_echo + 25 мкс (или +50 мкс)
```

### 🎛 3.7. Определение интервалов между пачками
Чтобы избежать наложения сигналов от предыдущих измерений, особенно переотражённых волн от крупных объектов или конструкций, вводится **пауза между пачками**.

- Ультразвук (40 кГц) распространяется со скоростью ~343 м/с, и при максимальной дистанции 2.7 м задержка сигнала может достигать **~8 мс**.
- С учётом возможных переотражений, рекомендуется **интервал между пачками ~15–25 мс**.
- Это соответствует **частоте 40–65 Гц**, чего достаточно для плавного ручного сканирования.

> Для сравнения, человеческий глаз воспринимает движение как непрерывное уже при частоте выше 25 Гц, а в системах визуального отображения стандартной считается частота 50–60 Гц. Это означает, что 40–65 замеров в секунду вполне достаточно для построения детальной временной или пространственной карты при перемещении ПЭП вручную.

### ✅ 3.8. Результат

- Система корректно различает фронт и пиковое срабатывание
- Компенсирует ошибки за счёт адаптации
- Сохраняет точность даже при слабом приёме
- Обеспечивает достоверную временную метку для расчёта координаты
- Остаётся надёжной и достаточно быстрой для большинства задач позиционирования при НК.

---


## 4. Сохранение и анализ данных с TDC7200 (на Arduino Due)

После завершения измерения времени между сигналами START и STOP, микроконтроллер Arduino Due получает от TDC7200 цифровое значение, соответствующее интервалу времени. В этом разделе описано, как происходит получение, преобразование и анализ этих данных на Arduino.

### 📥 4.1. Чтение данных от TDC7200
Микросхема TDC7200 измеряет интервал между первым фронтом сигнала `START` и первым обнаруженным фронтом `STOP`. Эти значения хранятся в регистре `TIME1` (в режиме 2 доступен только один STOP).

Пример кода для чтения регистра `TIME1` по SPI:

```cpp
uint32_t readTDC_TIME1() {
  uint32_t time1 = 0;
  digitalWrite(TDC_CS_PIN, LOW);
  SPI.transfer(0x10); // Адрес регистра TIME1
  time1 |= (uint32_t)SPI.transfer(0x00) << 16; // Считываем старший байт (MSB) и сдвигаем его влево на 16 бит
  time1 |= (uint32_t)SPI.transfer(0x00) << 8;  // Считываем средний байт и сдвигаем влево на 8 бит
  time1 |= (uint32_t)SPI.transfer(0x00);       // Считываем младший байт (LSB)
  digitalWrite(TDC_CS_PIN, HIGH);
  return time1;
}
```

### 📐 4.2. Перевод в единицы времени и расстояния
После получения значения из TDC7200 его нужно перевести в наносекунды и далее — в миллиметры. В режиме 2, при частоте 8 МГц, шаг составляет **55 пс**:

```cpp
const float LSB_ns = 55.0; // 55 пикосекунд на шаг
const float speed_of_sound = 343.0; // м/с, можно корректировать по температуре/влажности

uint32_t tdc_time = readTDC_TIME1();
float t_echo_ns = tdc_time * LSB_ns;
float t_echo_us = t_echo_ns / 1000.0;
float distance_m = (t_echo_us / 1e6) * speed_of_sound;
float distance_mm = distance_m * 1000.0;
```

> Важно: значение функции `micros()` можно использовать только для контроля времени между циклами. Для расчёта времени прохождения импульса используется только значение из регистра `TIME1`.

### 🧠 4.3. Логика анализа сигнала
Контроллер также может анализировать:

- 📊 **Количество принятых импульсов** — например, 10 из 10 или меньше;
- 📏 **Интервалы между STOP-сигналами** — сравниваются с ожидаемыми (например, 25 мкс при 40 кГц);
- 🔁 **Завершение пачки** — если интервал между импульсами превышает 50 мкс;
- ⚡ **Хвостовая инерция сигнала** — при искажении импульса возможно ложное срабатывание компаратора (задержка меньше 20 мкс — игнорировать);
- 🛠 **Коррекция** — если импульсов < 9, добавить +25 мкс к времени (учесть, что первый пропущен);
- 🧹 **Фильтрация** — отбрасывание пиков, где интервал между STOP-сигналами < 20 мкс (считается реакцией на “хвост”).

Также возможна адаптация длины пачки и уровня порога по результатам первых пробных запусков.

### 💾 4.4. Сохранение и передача
Полученные данные можно сохранять в массив, передавать через UART/USB, использовать для построения карты положения преобразователя, логирования и обучения модели дефектов.

Пример сохранения:

```cpp
struct Measurement {
  float t_us;
  float d_mm;
  uint8_t pulses_detected;
};
```

---


### 5. Определение скорости звука в воздухе

Для точного расчёта координат требуется учитывать скорость звука, зависящую от условий окружающей среды. Она определяется по температуре и влажности воздуха:

**Формула с учётом температуры и влажности:**
`v = 331.3 + 0.606·T + 0.0124·RH`

Где:

* `T` — температура (°C)
* `RH` — относительная влажность (%)

**Рекомендуемые датчики для Arduino Due:**

| Параметр           | Датчик        | Интерфейс | Примечания                                    |
| ------------------ | ------------- | --------- | --------------------------------------------- |
| Температура        | DS18B20       | 1-Wire    | Просто, стабильно, ±0.5 °C                    |
| Темп. + влажн.     | SHT31 / SHT35 | I²C       | Точные, быстрые, компактные                   |
| Темп./влажн./давл. | BME280        | I²C / SPI | Универсальный, подходит для расширенных задач |

**Пример кода для SHT31:**

```cpp
#include <Wire.h>
#include "Adafruit_SHT31.h"
Adafruit_SHT31 sht31 = Adafruit_SHT31();

void setup() {
  Serial.begin(9600);
  if (!sht31.begin(0x44)) {
    Serial.println("SHT31 not found");
    while (1) delay(1);
  }
}

void loop() {
  float temperature = sht31.readTemperature();
  float humidity = sht31.readHumidity();
  float v = 331.3 + 0.606 * temperature + 0.0124 * humidity;
  Serial.print("Speed of sound: ");
  Serial.print(v);
  Serial.println(" m/s");
  delay(1000);
}
```

---


## 6. Применения в контроле и позиционировании

### 6.1. Один канал — позиционирование системы TOFD или УЗ-сканера
Один канал может быть использован для определения положения мобильной измерительной системы, такой как:

- система **TOFD** (Time-of-Flight Diffraction);
- **ручной ультразвуковой сканер на фазированных решётках**.

В этом случае при каждом измерении фиксируются координаты положения устройства. Это даёт возможность для **картографирования результатов** вдоль шва или в каждом сечении сварного соединения.

> Такой подход позволяет пространственно соотнести измеренные данные и точно отображать их на карте обследуемой области.

### 6.2. Два канала — определение положения на плоскости
При использовании двух приёмников, расположенных по разные стороны сварного шва, можно определить координату ультразвукового преобразователя относительно центра шва.

Методы расчёта включают:
- **пересечение окружностей** по известным расстояниям;
- **триангуляцию** по задержкам сигнала;
- **пересечение гипербол** (для более высокой точности и устойчивости в условиях шума).

Также возможна оценка толщины с разных сторон от шва с использованием геометрического анализа. Для этого два или четыре приёмника могут фиксировать время прихода сигнала от излучателя, установленного на преобразователе, и определять его координаты с высокой точностью.

### 6.3. 6–8 каналов — позиционирование вокруг окружности трубы
При контроле сварных соединений труб необходимо знать положение преобразователя на цилиндрической поверхности.

Расположив 6–8 приёмников вокруг окружности, можно определить координаты излучателя в **цилиндрической системе координат**, используя разность времён прихода сигнала на каждый из приёмников.

> Это особенно актуально при полном сканировании труб или сосудов под давлением, где требуется контроль по всей окружности.

#### 6.4. Сводная таблица применения

| Кол-во каналов | Применение                                                             | Особенности                                                   |
|----------------|------------------------------------------------------------------------|----------------------------------------------------------------|
| 1              | TOFD, фазированные решётки                                             | Без позиционирования, фиксация времени прихода                |
| 2              | Позиционирование по оси шва, измерение толщины                         | Триангуляция или пересечение радиусов дальности               |
| 3–4            | Уточнение координат, фильтрация шумов                                  | Пересечение гипербол, повышенная устойчивость                 |
| 6–8            | Позиционирование по окружности (например, трубы)                       | Цилиндрическая система координат, полное покрытие             |

---


## 7. Алгоритмы и логика работы

Система построена на принципе автоматического запуска измерений с помощью сигнала `INTB` от TDC7200. Это позволяет достигать высокой частоты измерений без программных задержек. Ниже представлена сводная таблица с описанием ключевых этапов логики работы:

| Этап                         | Описание                                                                                   |
|-----------------------------|--------------------------------------------------------------------------------------------|
| 1. Формирование START       | Первый `START` подаётся от Arduino; далее новые `START` инициируются по `INTB` (через логику AND+OR). |
| 2. Подача пачки импульсов   | Генерация от 3 до 12 импульсов 40 кГц; длительность и количество адаптируются под условия. |
| 3. Приём сигнала            | Компаратор фиксирует фронт сигнала и подаёт `STOP` в TDC7200.                              |
| 4. Анализ интервалов        | Arduino сравнивает интервалы между импульсами: >50 мкс → конец цикла.                      |
| 5. Подсчёт импульсов        | Если получено 10/10 импульсов — данные точные; <10 — возможна компенсация на 25 мкс.       |
| 6. Проверка на "хвост"      | Анализ отклонений между интервалами, например, <22–23 мкс → фронт может быть поздним.      |
| 7. Адаптация порога         | Порог компаратора корректируется по стабильности числа импульсов и интервалов.            |
| 8. Расчёт координат         | По времени `STOP` и известной скорости звука вычисляется расстояние/позиция.              |
| 9. Сохранение/отправка      | Данные сохраняются и/или передаются в систему анализа/визуализации.                       |


> 💡 Промежуточные START-импульсы не зависят от кода Arduino и формируются логически, обеспечивая минимальную задержку.

---


## ✅ 8. Преимущества системы

Предложенная система позиционирования ультразвукового преобразователя на базе TDC7200 проектируется с учетом следующих технических и функциональных преимуществ:

### 🎯 8.1. Высокая точность
- Разрешение до **55 пикосекунд** благодаря TDC7200, должна обеспечить высокую точность


### 🚀 8.2. Высокая частота измерений
- Аппаратный автозапуск через **INTB** без участия CPU.
- Частота повторения измерений — **40–65Гц** что достаточно для плавного ручного сканирования.

### 🧠 8.3. Адаптивное управление чувствительностью
- Порог срабатывания компаратора регулируется **DAC** в реальном времени.
- Подстраивается в зависимости от количества принятых импульсов и интервалов между ними.

### 🎛 8.4. Управление длиной пачки
- Контроллер адаптирует количество возбуждающих импульсов:
  - **Короткие расстояния** → меньше импульсов (например, 3–5) ⇒ меньше мёртвая зона.
  - **Большие расстояния** → больше импульсов (например, 8–12) ⇒ лучше обнаружение.
- Возможна предварительная серия **пробных** измерений для автонастройки.

### 🌡 8.5. Учет условий среды
- Подключение датчика **температуры и влажности** (например, SHT31) по **I²C**.
- Расчёт **скорости звука с учётом окружающей среды** ⇒ более точное позиционирование.

### 🔧 8.6. Простота масштабирования
- Архитектура легко расширяется от **1 до 8 и более каналов**.
- Одинаковая логика работы и повторяемость схемы.

### 🧩 8.7. Гибкость интеграции
- Подходит для различных методов контроля:
  - TOFD,
  - ультразвуковые сканеры на фазированных решётках,
  - триангуляция и пересечение радиусов дальности и гипербол от разности прихода сигнала по различным приемникам,
  - позиционирование по окружности.
- Может использоваться как **самостоятельная подсистема позиционирования** в составе более крупного комплекса НК.

---


## ⚠️ 9. Ограничения системы

Несмотря на преимущества, предлагаемая архитектура системы имеет потенциальные ограничения, которые необходимо учитывать при проектировании и использовании.

### 📏 9.1. Ограниченный диапазон измерений
- **Максимальное расстояние** ограничено временем измерения в режиме 2 (до 8 мс), что соответствует примерно **2.7 м** в воздухе.
- **Минимальное расстояние** зависит от длительности пачки и системных задержек:
  - При 10 импульсах ~225–250 мкс ⇒ минимальное расстояние ≈ **80 мм**.
  - При 5 импульсах ⇒ можно снизить до **40 мм**.
- При слишком малом расстоянии первый импульс может попасть в «мёртвую зону» и не зафиксироваться, а при превышении 8 мс приём будет проигнорирован.
- Требуется **адаптация количества импульсов** в зависимости от расстояния.

### 🔁 9.2. Необходимость калибровки
- Скорость звука может варьироваться в зависимости от температуры, влажности, атмосферного давления.
- Даже при наличии датчиков окружающей среды, **требуется начальная калибровка**:
  - Установка начальной точки,
  - Сравнение с эталонной длиной,
  - Коррекция расчётов на практике.

### 📐 9.3. Геометрическая привязка и выбор системы координат
- Для точного позиционирования необходима **геометрическая модель**:
  - Прямая линия (1D),
  - Плоскость (2D),
  - Цилиндрическая система (например, труба),
- Также важно учесть начальные смещения: система должна знать, где находится **нулевая точка** и направление координат.
- Алгоритмы расчёта различаются для гипербол, триангуляции или радиусов.

### 🧭 9.4. Позиционирование приёмников
- Расстояния между приёмниками должны быть **известны с высокой точностью** (например, 0.1–0.5 мм).
- Приёмники должны быть установлены **жёстко и стабильно**, исключая сдвиги и вибрации.
- При изменении конфигурации системы требуется повторная калибровка.

### 🔍 9.5. Прямая видимость между излучателем и приёмниками
- Ультразвуковые волны (40 кГц) распространяются **по прямой линии**.
- Между излучателем и приёмником не должно быть:
  - Частей тела оператора,
  - Посторонних предметов,
  - Краёв конструкции (например, труб, стен).
- Угол излучения должен перекрывать зону чувствительности приёмников.

### 🌀 9.6. Влияние условий эксплуатации
- Несмотря на адаптацию порога и фильтрацию, сильные **шумы, вибрации**, резкие удары по конструкции могут исказить сигнал.
- Приёмники, установленные на вибрирующих трубах, требуют демпфирования или компенсации в алгоритме.
- Также важно учитывать **акустические переотражения** от стен, сварных усилений, швов.

### 🌪 9.7. Влияние внешней среды
- **Ветер** может отклонять акустическую волну, особенно на расстояниях >1.5 м.
- **Осадки (дождь, снег)** создают акустические помехи и затухание.
- Рекомендуется:
  - Работа в защищённых условиях,
  - Применение экранирующих кожухов,
  - Автоматическая оценка надёжности измерения по качеству пачки.

---


## Заключение

Предложенная система на базе микросхемы **TDC7200** реализует **иной подход** к определению координат при ручном неразрушающем контроле. В отличие от традиционных решений на базе механических энкодеров или анализа отражённого сигнала, предложенная система использует **внешние ультразвуковые приёмники и излучатель**, работающие в воздушной среде. Благодаря этому позиционирование может происходить **независимо от основной измерительной системы**, работающей в диапазоне 1–15 МГц.

Система построена на расчётах и принципах работы микросхемы TDC7200, адаптивной логики запуска, управления порогом срабатывания компаратора и учёта условий среды. Теоретически она способна обеспечивать определение положения преобразователя в пределах до 2.7 м с точностью до миллиметров. Однако, как и в случае с любой экспериментальной разработки, **реальная точность и устойчивость системы будут определяться условиями эксплуатации и должны быть подтверждены практическими испытаниями**.

Несмотря на возможные ограничения и «подводные камни», описанная архитектура демонстрирует потенциал для создания **модульной, расширяемой и малозависимой от механики системы позиционирования**. Это может существенно повысить информативность УЗ-контроля, особенно при ручном сканировании, и открыть путь к интеграции позиционирования в существующие средства НК без значительной переделки аппаратной части. 

---

## 📎 Связанные материалы

- [🛠 Реализация аппаратной части системы](https://github.com/du-dim/Ultrasonic-Emitter-Positioning-System/blob/main/hardware.md)

---


## ✍ Автор

**Дмитрий Дубовик**  
Инженер по неразрушающему контролю  
**Дата:** 17.05.2025  
**Email/контакт:** [dzm.dubovik@gmail.com]  
**GitHub:** [@du-dim](https://github.com/du-dim)

*Подготовлено как технический обзор и авторский проект.*  
*Использование возможно в рамках производственных и исследовательских задач.*  
*Проект находится в развитии — на этапе проектирования электроники и печатных плат.*

