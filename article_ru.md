### Система позиционирования ультразвукового излучателя на базе TDC7200 с адаптивной логикой и управлением порогом 

### 📌 Аннотация
Ультразвуковой контроль (UT) — метод неразрушающего контроля, широко применяемый в промышленности, особенно для контроля сварных соединений и измерения толщины. Одним из ключевых ограничений традиционного ультразвукового контроля с наклонным преобразователем, особенно при ручном сканировании является отсутствие локализации или точной локализации положения пьезоэлектрического преобразователя (ПЭП) в пространстве. Для решения этой задачи разработана система позиционирования ПЭП, позволяющая регистрировать координаты  ПЭП при каждом измерении. Это открывает новые возможности в интерпретации отражённых сигналов, повышает точность обнаружения дефектов, снижает количество ложных срабатываний и даёт возможность картографирования областей контроля.

### Введение

Позиционирование ультразвукового пьезоэлектрического преобразователя (ПЭП) в пространстве критично при неразрушающем контроле сварных соединений и в задачах, где важно отличить реальные дефекты от ложных отражений. Для решения этой задачи разработана система, обеспечивающая точное определение времени прихода ультразвукового сигнала (40 кГц) от излучателя, установленного на ПЭП, к приёмнику, размещённому вне зоны контроля (излучатель и приёмник направлены так, чтобы приёмник регистрировал фронт волны напрямую от излучателя).

Аппаратная часть системы основана на микросхеме **TDC7200**, обеспечивающей разрешение измерения временных интервалов до **55 пикосекунд**, что позволяет с высокой точностью фиксировать задержки между сигналами START и STOP.  
Источник: [TI TDC7200 Datasheet](https://www.ti.com/lit/gpn/TDC7200)

Так как стандартные возможности микросхемы TDC7200 по числу STOP-сигналов и диапазону времени ограничены, для расширения потенциала используется **режим 2**, в котором возможно измерение временных интервалов до **8 мс** после события START. Это соответствует расстоянию до ~2,7 м в воздухе (в зависимости от температуры и влажности).

Первый импульс `START` формируется микроконтроллером (в данной реализации — **Arduino DUE**). Последующие импульсы START, необходимые для подсчёта количества принятых ультразвуковых импульсов и анализа интервалов между ними, формируются **аппаратно** — на основе сигнала `INTB`, который формируется после завершения каждого измерения. Этот сигнал проходит через **инвертор** и логический элемент **AND (И)**, а затем объединяется через **OR (ИЛИ)** с сигналом от контроллера.

> Такая схема исключает программные задержки между измерениями и особенно эффективна при высокой частоте опроса.

Порог срабатывания компаратора задаётся через `DAC` микроконтроллера и адаптируется под текущие условия, на основе анализа:
- количества принятых импульсов,
- временных интервалов между ними.

Например, при частоте **40 кГц** интервал между импульсами составляет около **25 мкс**.  
Если интервал между STOP-сигналами превышает **50 мкс**, цикл измерения считается завершённым.  
Настройка порога позволяет:
- при низком пороге — повышать чувствительность,
- при высоком — отбрасывать слабые и шумовые сигналы.

В системе дополнительно используется цифровой датчик **температуры и влажности** (например, **SHT31**) по интерфейсу I²C. Это позволяет точно рассчитывать скорость звука в воздухе с учётом обоих параметров окружающей среды.

> Это особенно важно при точных расчётах расстояния, так как скорость звука чувствительна к температуре и влажности воздуха.

Также стоит отметить, что **вывод TRIGG** микросхемы TDC7200 в данной архитектуре **не используется**. Все циклы измерений управляются через сигнал `START` и обработку INTB, а выбор режима 2 позволяет отказаться от дополнительных временных ограничений, связанных с TRIGG.

![Block Diagram: Microcontroller-Centric Architecture](https://github.com/user-attachments/your-detailed-architecture-diagram.png)
*Рис. 1. Управляющая архитектура: связи между Arduino Due, TDC7200, компаратором, логикой и датчиком температуры/влажности*

---

### 1. Микроконтроллер и архитектура системы

Центральное управление системой осуществляет микроконтроллер, в частности, плата **Arduino Due**, выбранная за счёт высокой скорости обработки, большого количества цифровых выводов и встроенного 12-битного **ЦАП (DAC)**.

Функции микроконтроллера включают:

- **SPI-интерфейс** — конфигурация и чтение результатов с TDC7200.
- **ЦАП (DAC)** — формирует опорное напряжение для компаратора, определяя порог срабатывания.
- **I²C-интерфейс** — используется для подключения цифрового датчика температуры и влажности (например, SHT31), что позволяет учитывать влияние окружающей среды на скорость звука.
- **Цифровые выводы** — задействованы для генерации пачек возбуждающих импульсов, управления логикой запуска TDC и обработки сигнала `INTB`.

На аппаратном уровне реализована минимальная логическая схема, включающая **инвертор**, **логический элемент И** и **логический элемент ИЛИ**, позволяющая формировать сигнал `START` как от Arduino, так и автоматически — по окончанию предыдущего измерения (сигнал `INTB` от TDC7200).

Такой подход позволяет:

- исключить задержки между измерениями;
- адаптировать порог компаратора в зависимости от условий;
- масштабировать систему для работы с несколькими каналами (приёмниками).

> 🛈 Общая схема архитектуры приведена в конце раздела «Введение» (Рис. 1).

---

### 2. Работа с пачкой ультразвуковых сигналов

Контроллер (например, Arduino Due) формирует пачку из 5–10 импульсов для возбуждения ПЭП. Один канал подходит для TOFD, фазового анализа и базового контроля. Подсчёт количества принятых импульсов позволяет определить, был ли первый ослаблен. Если, например, зафиксировано 9 импульсов из 10, система добавляет 25 мкс к расчётному времени. Если 10 или больше — считается, что первый импульс принят корректно.

Автоматический старт от INTB особенно важен для точного определения момента прихода сигнала, а также для формирования последовательности замеров без программной задержки. Это позволяет запускать новый цикл измерения каждые 60 мкс.

**Формула расчёта времени прихода:**
`t_echo = t_STOPx - t_START1`

**\[Insert Figure: Pulse train and interval timing diagram]**

---

### 3. Применения в контроле и позиционировании

#### 3.1 Один канал — TOFD, контроль толщины, отражённые сигналы

Применим для базовых измерений или TOFD. Также возможна регистрация отражённых сигналов, если известно положение приёмника и время пролёта.

#### 3.2 Два канала — определение положения вдоль сварного шва

При установке двух приёмников с двух сторон шва можно рассчитывать положение ПЭП относительно оси шва. Расчёт — по гиперболе, окружностям или через треугольник.

**\[Insert Figure: Weld with two receivers and emitter path]**

#### 3.3 6–8 каналов — определение координат на трубе

Для контроля по окружности трубы применяется до 8 приёмников. Сигнал от излучателя приходит с различными задержками, что позволяет определить его координаты в цилиндрической системе. Это актуально для выявления дефектов в любой точке окружности.

**\[Insert Figure: Circular receiver placement and calculation principle]**

---

### 4. Алгоритмы и логика работы

* Режим TDC7200 с многократными TRIGG
* INTB управляет автозапуском нового измерения
* Логика: AND (сигнал INTB + флаг EN), NOT, OR
* Сравнение интервалов между импульсами — защита от шумов
* Завершение цикла при длительном интервале

Вся система работает без участия микроконтроллера в промежуточных циклах. Это снижает нагрузку на CPU и увеличивает точность фиксации момента прихода сигнала.

---

### 5. Определение скорости звука в воздухе

Для точного расчёта координат требуется учитывать скорость звука, зависящую от условий окружающей среды. Она определяется по температуре и влажности воздуха:

**Формула с учётом температуры и влажности:**
`v = 331.3 + 0.606·T + 0.0124·RH`

Где:

* `T` — температура (°C)
* `RH` — относительная влажность (%)

**Рекомендуемые датчики для Arduino Due:**

| Параметр           | Датчик        | Интерфейс | Примечания                                    |
| ------------------ | ------------- | --------- | --------------------------------------------- |
| Температура        | DS18B20       | 1-Wire    | Просто, стабильно, ±0.5 °C                    |
| Темп. + влажн.     | SHT31 / SHT35 | I²C       | Точные, быстрые, компактные                   |
| Темп./влажн./давл. | BME280        | I²C / SPI | Универсальный, подходит для расширенных задач |

**Пример кода для SHT31:**

```cpp
#include <Wire.h>
#include "Adafruit_SHT31.h"
Adafruit_SHT31 sht31 = Adafruit_SHT31();

void setup() {
  Serial.begin(9600);
  if (!sht31.begin(0x44)) {
    Serial.println("SHT31 not found");
    while (1) delay(1);
  }
}

void loop() {
  float temperature = sht31.readTemperature();
  float humidity = sht31.readHumidity();
  float v = 331.3 + 0.606 * temperature + 0.0124 * humidity;
  Serial.print("Speed of sound: ");
  Serial.print(v);
  Serial.println(" m/s");
  delay(1000);
}
```

---

### 6. Преимущества

* Высокая частота измерений (до 25–30 в секунду)
* Точность до 0.5 мм на расстоянии 2.5 м
* Отсутствие программной задержки между циклами
* Аппаратное управление логикой, не зависящее от SPI
* Поддержка масштабируемости: от 1 до 8 каналов

---

### Заключение

Разработанная система на базе TDC7200, компаратора и простой логической схемы обеспечивает высокоточную фиксацию ультразвуковых импульсов. Адаптивный порог, анализ фронтов и интервальный контроль позволяют применять её в реальных задачах НК — от TOFD до контроля сварки и контроля по окружности. Благодаря автоматическому запуску измерений и работе с пачками сигналов, система устойчиво определяет положение ПЭП даже в шумных условиях.

**\[Insert Figure: Application examples and positioning accuracy map]**

---

*Prepared for open access publication. For diagrams, source code and reference hardware, see GitHub release \[link to be added].*
